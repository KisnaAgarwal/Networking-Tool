# NetworkGuardian

A simple network utility for monitoring and interacting with hosts.

## Build

```bash
mkdir -p build
cd build
cmake ..
cmake --build .
```

## Run

```bash
./NetworkGuardian
```

## Notes

- Ensure you run the binary from the `build` directory or provide the correct path.
- Adjust permissions as needed for network access.
 